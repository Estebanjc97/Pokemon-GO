using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.XR.ARFoundation;
//using UnityEngine.XR.ARSubsystems;

public class PlaceToObject : MonoBehaviour
{
    //[SerializeField]
    //ARRaycastManager arRaycastManager;

    //static List<ARRaycastHit> _hits = new List<ARRaycastHit>();

    //public GameObject objectToPlace;

    //private void Update()
    //{
    //    if(Input.touchCount>0)
    //    {
    //        Touch touch = Input.GetTouch(index: 0);

    //        if(touch.phase==TouchPhase.Began)
    //        {
    //            if (arRaycastManager.Raycast(touch.position, _hits, trackableTypes: TrackableType.PlaneWithinPolygon))
    //            {
    //                Pose hitPose = _hits[0].pose;

    //                Instantiate(objectToPlace, hitPose.position, hitPose.rotation);
    //            }
    //        }
    //    }

    //}
}
